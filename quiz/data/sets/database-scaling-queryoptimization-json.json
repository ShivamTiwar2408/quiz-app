{"questions": [{"question": "What is query optimization?", "options": [{"key": "a", "text": "Writing shorter queries"}, {"key": "b", "text": "The process of improving query performance through better execution plans, indexes, and query structure"}, {"key": "c", "text": "Deleting slow queries"}, {"key": "d", "text": "Using faster hardware"}], "correct": ["b"], "explanation": "Query optimization: analyze and improve how queries execute. Involves: query rewriting, index usage, execution plan analysis, statistics updates. Goal: minimize I/O, CPU, and response time."}, {"question": "What is an execution plan (query plan)?", "options": [{"key": "a", "text": "A project timeline"}, {"key": "b", "text": "The database's strategy for executing a query—which indexes to use, join order, scan types"}, {"key": "c", "text": "A backup schedule"}, {"key": "d", "text": "A user permission list"}], "correct": ["b"], "explanation": "Execution plan: database's roadmap for query execution. Shows: table scans vs index seeks, join algorithms, sort operations, estimated costs. Use EXPLAIN/EXPLAIN ANALYZE to view. Essential for optimization."}, {"question": "What is the difference between a table scan and an index seek?", "options": [{"key": "a", "text": "They are the same"}, {"key": "b", "text": "Table scan reads all rows; index seek uses index to directly access specific rows"}, {"key": "c", "text": "Index seek is always slower"}, {"key": "d", "text": "Table scan uses less memory"}], "correct": ["b"], "explanation": "Table scan: O(n), reads every row. Index seek: O(log n), uses B-tree index to find rows directly. For selective queries (few rows), index seek is much faster. For large result sets, scan might be better."}, {"question": "What is a covering index?", "options": [{"key": "a", "text": "An index that covers the table"}, {"key": "b", "text": "An index that contains all columns needed by a query, avoiding table lookups"}, {"key": "c", "text": "A backup index"}, {"key": "d", "text": "An encrypted index"}], "correct": ["b"], "explanation": "Covering index: includes all columns the query needs. Query satisfied entirely from index—no table access. Example: INDEX(user_id, name, email) covers SELECT name, email WHERE user_id=1."}, {"question": "Why is SELECT * often bad for performance?", "options": [{"key": "a", "text": "It's not bad"}, {"key": "b", "text": "Fetches unnecessary columns, prevents covering indexes, increases I/O and network transfer"}, {"key": "c", "text": "It's too fast"}, {"key": "d", "text": "It causes syntax errors"}], "correct": ["b"], "explanation": "SELECT * problems: fetches unneeded data, can't use covering indexes, more I/O, more network transfer, breaks if schema changes. Always select only needed columns."}, {"question": "What is the N+1 query problem?", "options": [{"key": "a", "text": "A mathematical formula"}, {"key": "b", "text": "Fetching a list (1 query) then fetching related data for each item (N queries) instead of using joins"}, {"key": "c", "text": "Having too many indexes"}, {"key": "d", "text": "A network issue"}], "correct": ["b"], "explanation": "N+1: SELECT users (1 query), then for each user SELECT orders WHERE user_id=X (N queries). Fix: JOIN in single query, or use IN clause. Common ORM pitfall. Devastating at scale."}, {"question": "What does EXPLAIN ANALYZE do?", "options": [{"key": "a", "text": "Deletes the query"}, {"key": "b", "text": "Executes the query and shows actual execution statistics vs estimates"}, {"key": "c", "text": "Creates an index"}, {"key": "d", "text": "Backs up the database"}], "correct": ["b"], "explanation": "EXPLAIN: shows planned execution. EXPLAIN ANALYZE: actually runs query, shows real times, row counts vs estimates. Reveals: bad estimates, slow operations, missing indexes. Essential debugging tool."}, {"question": "What is query caching?", "options": [{"key": "a", "text": "Storing queries in files"}, {"key": "b", "text": "Storing query results so identical queries return cached results without re-execution"}, {"key": "c", "text": "Compressing queries"}, {"key": "d", "text": "Encrypting queries"}], "correct": ["b"], "explanation": "Query cache: store result sets keyed by query text. Identical query hits cache. Challenges: cache invalidation on writes, memory usage, doesn't help unique queries. Often disabled in modern systems."}, {"question": "Why might adding an index make writes slower?", "options": [{"key": "a", "text": "Indexes don't affect writes"}, {"key": "b", "text": "Every INSERT/UPDATE/DELETE must also update all relevant indexes"}, {"key": "c", "text": "Indexes compress data"}, {"key": "d", "text": "Indexes are read-only"}], "correct": ["b"], "explanation": "Index overhead: each write must update table AND all indexes. More indexes = slower writes. Balance: add indexes for read performance, but don't over-index. Profile your workload."}, {"question": "What is predicate pushdown?", "options": [{"key": "a", "text": "A wrestling move"}, {"key": "b", "text": "Moving filter conditions closer to data source to reduce data transferred between operations"}, {"key": "c", "text": "A backup technique"}, {"key": "d", "text": "An encryption method"}], "correct": ["b"], "explanation": "Predicate pushdown: apply WHERE filters as early as possible. Instead of: scan all → join → filter, do: filter → join. Reduces intermediate data. Optimizer does this automatically when possible."}, {"question": "What is the impact of functions on indexed columns in WHERE clauses?", "options": [{"key": "a", "text": "No impact"}, {"key": "b", "text": "Prevents index usage—WHERE YEAR(date_col) = 2024 can't use index on date_col"}, {"key": "c", "text": "Makes queries faster"}, {"key": "d", "text": "Creates new indexes"}], "correct": ["b"], "explanation": "Functions on columns prevent index use: WHERE UPPER(name) = 'JOHN' scans table. Fix: WHERE name = 'john' (if case-insensitive collation), or create functional index. Keep indexed columns 'naked'."}, {"question": "A query joining 5 tables is slow. What's the first optimization step?", "options": [{"key": "a", "text": "Add more RAM"}, {"key": "b", "text": "Run EXPLAIN to understand the execution plan and identify bottlenecks"}, {"key": "c", "text": "Delete some tables"}, {"key": "d", "text": "Rewrite in a different language"}], "correct": ["b"], "explanation": "First step: understand what's happening. EXPLAIN shows: join order, index usage, row estimates. Identify: table scans (need index?), bad join order, incorrect estimates. Then optimize specifically."}]}